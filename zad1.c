#include <stdio.h>
#include <math.h>

int main()
{
    double liczba, sqrta;
    scanf("%if", &liczba);
    sqrta=sqrt(liczba);

    printf("po spierwiastkowaniu %.2if",sqrta);
    return 0;
}
